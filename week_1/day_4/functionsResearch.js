// phil welsby - 28 july 2021

// research functios

// write my first function
function square(num) {
  return num**2;
}

// call function and send to stdout
console.log(square(12));

function squareRoot(num) {
  return Math.sqrt(num);
}

// call function and send to stdout
console.log(squareRoot(9));

